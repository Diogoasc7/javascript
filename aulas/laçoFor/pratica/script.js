function contar(){
        let soma = 0;
        for (let c = 1; c <= 1000; c++) {
            soma = soma + c // Acumula a soma dos números de 1 a 1000
            document.write(`Soma: ${soma}`)
        }
    } 